rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Global fallback & all collections allow read/write for active app usage
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

